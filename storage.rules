// Firebase Storage Security Rules
// This file should be deployed to Firebase Storage
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access to all paths (for testing)
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}